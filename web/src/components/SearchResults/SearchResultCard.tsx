import * as React from "react";
import { RouteComponentProps } from 'react-router-dom';

interface Props extends RouteComponentProps<any> { }

const SearchResultCard: React.FC<any> = (props) => {
    return (
        <div></div>
    );
}

export default SearchResultCard;